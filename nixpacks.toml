[phases.setup]
nixPkgs = ["nodejs", "chromium"]

[phases.install]
cmds = ["npm install", "npx playwright install chromium"]

[phases.build]
cmds = ["npx playwright install-deps chromium"]

[start]
cmd = "npm start"
